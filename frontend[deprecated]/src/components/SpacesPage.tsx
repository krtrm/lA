import React from 'react';
import PageContent from '../pages/SpacesPage';

export default function SpacesPage() {
  // Instead of redirecting, simply render the page content directly
  return <PageContent />;
}
